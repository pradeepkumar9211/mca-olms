<!-- This will be deleted before finalizing -->
<!-- About the Git line-ending config

Yes — run the commands on both OS machines, but with their respective settings:

Linux
git config --global core.autocrlf input
Windows
git config --global core.autocrlf true -->